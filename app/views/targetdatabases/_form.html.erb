<%= form_for(@targetdatabase) do |f| %>
  <% if @targetdatabase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@targetdatabase.errors.count, "error") %> prohibited this targetdatabase from being saved:</h2>

      <ul>
      <% @targetdatabase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
